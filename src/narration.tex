\documentclass{article}

\usepackage{
	arxiv,
	amsmath,
	amssymb,
	bm,
	cleveref
}

\title{Computing the Metric Dimension of Graphs using Satisfiability Modulo Theories}

\author{
	Fikri Yudhistira Ajiwijaya \\
	Institut Teknologi Sepuluh Nopember
	\And
	Muhammad Syifa'ul Mufid \\
	Institut Teknologi Sepuluh Nopember
}

\begin{document}
	\maketitle

	\section{Introduction}

	The theoretical part of this paper is divided into three major parts. The first of which consists of the heavy lifting of formally defining notations for slicing, dicing, and filtering of matrices, tuples, and sets. While it may seem trivial and verbose at first glance, it is of utmost importance due to the lack of standard notations for these basic operations. Part two and part three extensively relies on using these carefully defined operations, without which would be incomprehensible to read. This rigor is both necessary and unnegotiable. Part two consists of a quick recall on the metric dimension problem definition, then followed by the first major contribution of this paper; that is, the journey in perspective shift of metric dimension reformulated in terms of slicing, dicing, and filtering. After all this gruelling groundwork, we are finally able to introduce the theoretical part of our result in part three, where we formulate constraints for metric dimension in satisfiability modulo theories, through careful solution space analysis.

	\section{Slicing, Dicing, and Filtering}

	For any set \(U\), we denote its cardinality as \(|U|\) and its power set as \(\mathcal{P}\). Given a subset \(R \subseteq U\), its complement is defined as \(\widetilde{R} = U \backslash R\).

	For a \(k\)-tuple \(C\), we denote its length by \(|C|\) and refer to the element at the \(i\)-th position as \(C(i)\). We say that \(U\) has a correspondence with \(C\) when there exists a bijective function \(\mathrm{idx} : U \to \{1, \cdots, |C|\}\). This allows us to index \(C\) by elements of \(U\), where for any \(u \in U\), we define \(C(u) = C(\mathrm{idx}(u))\). Furthermore, for any subset \(\{u_1, \cdots, u_r\} \subseteq U\), we define the sub-tuple \(C(\{u_1, \cdots, u_r\}) = (C(u_1),\ \cdots,\ C(u_r))\), preserving element order of \(C\).

	We define some element-wise operations on \(k\)-tuples. The element-wise negation, \(\neg C = (\neg C(1), \cdots, \neg C(k))\). The element-wise disjunction, \(C_1 \vee C_2 = (C_1(1) \vee C_2(1), \cdots, C_1(k) \vee C_2(k))\). The element-wise equality, \(C_1\ \ddot{=}\ C_2 = (C_1(1) = C_2(1), \cdots, C_1(k) = C_2(k))\). We also define \((\mathrm{True})^k\) as the \(k\)-tuple consisting of all \(\mathrm{True}\) values.

	Given a Boolean \(k\)-tuple \(B\), where \(U\) has a correspondence with \(B\), we define the filter operation \(U(B) = \{u \in U : B(u) = \mathrm{True}\}\). Similarly, given an integer \(k\)-tuple \(I\) that \(U\) has a correspondence with, \(U(I) = \{u \in U : I(u) \neq 0\}\). We extend this operation for a set of Boolean \(k\)-tuples, \(U(\{B_1, B_2, \cdots\}) = \{U(B_1),\ U(B_2),\ \cdots\}\). Likewise, for a set of integer \(k\)-tuples, \(U(\{I_1, I_2, \cdots\}) = \{U(I_1),\ U(I_2),\ \cdots\}\).

	For a matrix \(A\) with \(n\) rows and \(m\) columns, we use \(|A|_{\mathrm{row}}\) to denote the number of rows in \(A\) and \(|A|_{\mathrm{col}}\) to denote the number of columns in \(A\). We define the notation \(\mathcal{A} \overset{\mathrm{row}}{\in} A\) to say that \(\mathcal{A}\) is a row in \(A\) taken as an \(m\)-tuple, and \(\mathcal{A} \overset{\mathrm{col}}{\in} A\) to say that \(\mathcal{A}\) is a column in \(A\) as taken an \(n\)-tuple. We refer to the element at the \(i\)-th row and \(j\)-th column with \(A(i, j)\), the \(i\)-th row taken as an \(m\)-tuple with \(A(i, \cdot)\), and the \(j\)-th column taken as an \(n\)-tuple with \(A(\cdot, j)\).

	We say that a set \(S\) has a row correspondence with \(A\) when there exists a bijective function \(\mathrm{idx} : S \to \{1, \cdots, |A|_{\mathrm{row}}\}\), and say that a set \(T\) has a column correspondence with \(A\) when there exists a bijective function \(\mathrm{idx} : T \to \{1, \cdots, |A|_{\mathrm{col}}\}\). This allows us to index the rows of \(A\) by elements of \(S\) and the columns of \(A\) by elements of \(T\), where for any \(s \in S\), we define \(A(s, \cdot) = A(\mathrm{idx}(s), \cdot)\), and for any \(t \in T\), we define \(A(\cdot, t) = A(\cdot, \mathrm{idx}(t))\).

	Finally, for any subset \(\{s_1, \cdots, s_r\} \subseteq S\), we define the submatrix by row selection as
	\[
		A(\{s_1, \cdots, s_r\}, \cdot) = \left[
		\begin{array}{c}
			A(s_1, \cdot) \\
			\vdots \\
			A(s_r, \cdot)
		\end{array}
		\right],
	\]
	preserving row order of \(A\). And for any subset \(\{t_1, \cdots, t_r\} \subseteq T\), we define the submatrix by column selection, as
	\(
		A(\cdot, \{t_1, \cdots, t_r\} \subseteq T) = \left[\enspace A(\cdot, t_1)\enspace \cdots\enspace A(\cdot, t_r)\enspace \right]
	\),
	preserving column order of \(A\).

	\section{Metric Dimension}

	Let \(G\) be a connected graph with \(n\) vertices \(V = \{v_1, \cdots, v_n\}\) and \(m\) edges \(E = \{e_1, \cdots, e_m\}\). There exists a function \(d(u, v)\) for any \(u, v \in V\), that gives the distance from \(u\) to \(v\). Then, it is defined \(d(u, e)\) for any \(u, v, w \in V\) and \(e \in E\) to be \(d(u, e) = \mathrm{min}(d(u, v),\ d(u, w))\) if \(e\) is the edge that connects \(v\) and \(w\).

	Let us take \(W = \{w_1, \cdots, w_k\} \subseteq V\). The representation of \(v \in V\) with respect to \(W\) is defined as \(r(v|W) = (d(v, w_1), \cdots, d(v, w_k))\). The edge representation of \(e \in E\) with respect to \(W\) is defined as \(r(e|W) = (d(e, w_1), \cdots, d(e, w_k))\). \(W\) is said to be a resolving set when all representations with respect to it are unique; that is, \(\forall u, v \in V, u \neq v, r(u|W) \neq r(v|W)\). Similarly, \(W\) is said to be an edge resolving set when all edge representations with respect to it are unique; that is, \(\forall e, f \in E, e \neq f, r(e|W) \neq r(f|W)\).

	When \(W\) is not a resolving set, \(W\) is called a non-resolving set. \(\mathcal{P}(V)\) is partitioned into the set of resolving sets and the set of non-resolving sets. Likewise, when \(W\) is not an edge resolving set, \(W\) is called an edge non-resolving set. \(\mathcal{P}(V)\) is also partitioned into the set of edge resolving sets and the set of edge non-resolving sets.

	The metric dimension of \(G\) is the minimum cardinality of its resolving sets, and the edge metric dimension of \(G\) is the minimum cardinality of its edge resolving sets.

	Observe that to show \(W\) is a resolving set, we practically must exhaustively calculate all representations against it. Let us define a structure that contains all of these necessary information: \(r(V|W) = \left[\ r(v_1|W)\ \cdots\ r(v_n|W)\ \right]\). This matrix has a dimension of \(|W| \times |V|\), and by construction, \(V\) has a column correspondence with \(r(V|W)\). All possible representations with respect to \(W\) are stored as its columns, such that, for any \(v \in V\), its representation with respect to \(W\) can be obtained by column indexing, \(r(v|W) = r(V|W)(\cdot, v)\). We are now able to redefine the resolving set criteria in terms of this matrix, \(\forall u, v \in V, u \neq v, r(V|W)(\cdot, u) \neq r(V|W)(\cdot, v)\), turning it into a matter of showing that no two columns in \(r(V|W)\) are the same.

	The exact same structure can also be made for the case of edge resolving set. Let us define \(r(E|W) = \left[\ r(e_1|W)\ \cdots\ r(e_m|W)\ \right]\), resulting in a matrix with a dimension of \(|W| \times |E|\), and \(E\) having column correspondence with it by construction. For any \(e \in E\), its representation with respect to \(W\) is \(r(e|W) = r(E|W)(\cdot, e)\), transforming the edge resolving set criteria into \(\forall e, f \in E, e \neq f, r(E|W)(\cdot, e) \neq r(E|W)(\cdot, f)\).

	\section{Distance Matrix}

	When \(W = V\), the matrix \(r(V|V)\) is more commonly known by the name of the distance matrix, which we denote here by \(D^{(V)}\), having a dimension of \(|V| \times |V|\). More formally, defined as
	\[
		D^{(V)} = \left[
		\begin{array}{cccc}
			d(v_1, v_1) & d(v_1, v_2) & \cdots & d(v_1, v_n) \\
			d(v_2, v_1) & d(v_2, v_2) & \cdots & d(v_2, v_n) \\
			\vdots & \vdots & \ddots & \vdots \\
			d(v_n, v_1) & d(v_n, v_2) & \cdots & d(v_n, v_n) \\
		\end{array}
		\right]
	\]
	where \(V\) have both row and column correspondence with it. This means that, for any \(W \subseteq V\), we can obtain \(r(V|W)\) directly by row indexing the distance matrix, \(r(V|W) = D^{(V)}(W, \cdot)\). Consequently, all possible representations in \(G\) are already contained within the distance matrix, and can be obtained through \(r(v|W) = D^{(V)}(W, \cdot)(\cdot, v)\), for any \(v \in V\) and any \(W \subseteq V\). Once again, we can now rewrite the resolving set criteria in terms of the distance matrix, \(\forall u, v \in V, u \neq v, D^{(V)}(W, \cdot)(\cdot, u) \neq D^{(V)}(W, \cdot)(\cdot, v)\).

	Likewise, the matrix \(r(E|V)\) is more commonly known by the name of the edge distance matrix, denoted by \(D^{(E)}\), with a dimension of \(|V| \times |E|\). It is formally defined as
	\[
		D^{(E)} = \left[
		\begin{array}{cccc}
			d(v_1, e_1) & d(v_1, e_2) & \cdots & d(v_1, e_m) \\
			d(v_2, e_1) & d(v_2, e_2) & \cdots & d(v_2, e_m) \\
			\vdots & \vdots & \ddots & \vdots \\
			d(v_n, e_1) & d(v_n, e_2) & \cdots & d(v_n, e_m) \\
		\end{array}
		\right]
	\]
	where \(V\) has a row correspondence and \(E\) has a column correspondence with it. And for any \(W \subseteq V\), we can obtain \(r(E|W) = D^{(E)}(W, \cdot)\). Therefore all edge representations in \(G\) are contained in this matrix, that can be obtained by \(r(e|W) = D^{(E)}(W, \cdot)(\cdot, e)\) for any \(e \in E\) and any \(W \subseteq V\). The edge resolving set criteria then becomes \(\forall e, f \in E, e \neq f, D^{(E)}(W, \cdot)(\cdot, e) \neq D^{(E)}(W, \cdot)(\cdot, f)\).

	\section{...\textit{blah blah blah}...}

	...\textit{blah blah blah}...

	\section{Experiments and Results}

	...\textit{blah blah blah}...

	\section{Conclusions and Future Works}

	...\textit{blah blah blah}...
\end{document}
